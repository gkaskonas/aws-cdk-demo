name: Test

on:
  push:
    branch: ["v*"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v2
        with:
          node-version: 16
      - run: yarn install --audit --frozen-lockfile
      - run: yarn test